import Header from "@/components/(auth)/Header";
import React from "react";

const Dashboard = () => {
  return (
    <div>
        <Header name={"Dashboard: User"} />
    </div>
  );
};

export default Dashboard;
